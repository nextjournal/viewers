(ns clerk.missing-title
  (:require nextjournal.devdocs))

;; This notebook's title in the navbar is inferred by its namespace according to

(nextjournal.devdocs/path->title "docs/clerk/missing_title.clj")
